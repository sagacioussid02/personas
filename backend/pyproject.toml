[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twin-backend"
version = "1.0.0"
description = "FastAPI backend for Personality Twin - Turn human expertise into an always-on AI persona"
requires-python = ">=3.12"
authors = [{name = "Personality Twin Team"}]
license = {text = "MIT"}

dependencies = [
    "fastapi==0.104.1",
    "uvicorn[standard]==0.24.0",
    "pydantic==2.5.0",
    "pydantic-settings==2.1.0",
    "python-dotenv==1.0.0",
    "boto3==1.29.7",
    "botocore==1.32.7",
    "httpx==0.25.2",
    "python-jose[cryptography]==3.3.0",
    "PyJWT==2.8.1",
    "cryptography==41.0.7",
    "requests==2.31.0",
]

[project.optional-dependencies]
dev = [
    "pytest==7.4.3",
    "pytest-asyncio==0.21.1",
    "black==23.12.0",
    "flake8==6.1.0",
    "mypy==1.7.1",
    "pip-tools==7.3.0",
]

[tool.setuptools]
packages = ["twin"]

[tool.black]
line-length = 100
target-version = ["py312"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
